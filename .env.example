# OpenAI API key — used for memory extraction (gpt-4o-mini) and embeddings (text-embedding-3-small)
OPENAI_API_KEY=sk-...

# Secret key agents must send as the X-Api-Key header on every request
MEM0_API_KEY=your-secret-key-here
